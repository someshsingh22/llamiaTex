
\section{Introduction}
Large language models (LLMs) are increasingly deployed as general-purpose orchestrators that coordinate tools and agents to solve complex tasks \citep{hong_metagpt_2024, tran_multi-agent_2025}.
A central pattern in this paradigm is collaboration with \emph{subagents}, agents that excel within a narrow domain \citep{anthropic_claude_subagents_2025,openai_codex_subagents_2025}.
This collaboration allows orchestrator LLMs to utilize the subagent's domain specific intelligence \citep{hong_metagpt_2024} and preserve its context length by task delegation \citep{zhang_chain_2024}, enabling effective multi-step reasoning and planning over long horizons.
Today, this collaboration is mediated entirely through natural language: the LLM invokes the subagent, receives a natural language description of its output, and reasons over that description to decide subsequent actions\citep{tran_multi-agent_2025}. 
Natural language collaboration is reasonable when both agents are themselves language models, as they share the same input space. 
However, in many important domains such as game playing, robotics, and  autonomous driving the strongest available agents are not language models; their expertise is encoded in internal representations: latent states capturing policy, value estimates, and learned features\citep{silver2017mastering,monroe2024mastering,brohan2022rt}.
This mismatch between LLMs and non language subagents's input space raises a fundamental question: 
\begin{quote}
\centering\emph{How can LLMs effectively collaborate and jointly reason with non-language subagents?}
\end{quote}

As LLMs increasingly serve as general purpose orchestrators, the depth of their collaboration with these subagents determines what these systems can ultimately achieve.
Effective collaboration has the potential to unlock creative applications that neither achieves alone.
Consider chess: LLMs have been trained on more chess literature than most experts will study in a lifetime, yet they cannot leverage it to play the game competently, trailing far behind modern engines and experts~\cite{XXXChessBench}.
Conversely, pretrained engines surpassed human grandmasters decades ago\citep{campbell2002deep}, yet they remain narrow specialists that are unable to explain the rationale behind a move or strategize under different contexts.
The most valued applications like commentating on a game, preparing against an opponent's known weaknesses, or making a creative puzzle therefore remain unsolved, because they demand both the engine's deep positional understanding and the LLM's ability to reason over human intent in natural language.
Improving how the LLM and the specialized subagent reason and collaborate would unlock these applications.

Existing approaches to LLM-subagent collaboration attempt to bridge this gap symbolically, by \emph{verbalizing} the subagent's outputs into natural language before passing them to the LLM.
Early work finetunes language models on textual descriptions of agent actions and value estimates \citep{zang2019automated, lee2022improving}, while more recent systems rely on in-context learning and tool-calling interfaces to surface subagent outputs at inference time \citep{schick2023toolformerlanguagemodelsteach, yao2022react, kim2024bridging}.
However, these approaches share a common assumption: that the subagent's expertise can be faithfully verbalized.
We argue that this assumption is fundamentally limiting.
A chess engine's latent representation of a state reflects millions of learned features like threats, tempo, pawn structure, and long-range tactical motifs as shown by \citet{jenner2024evidence} that cannot be losslessly compressed into a textual rationale.
Verbalization therefore forces the subagent's representations through a lossy bottleneck, collapsing rich latent structure into surface level descriptions.
Moreover, this error compounds across interactions, in multi-step settings, each exchange between the agents strips away details, and accumulates errors over the reasoning horizon, eroding the very benefits that motivated the collaboration in the first place.
Consequently, existing methods prevent orchestrator LLMs from fully exploiting the capabilities encoded in non-language subagents.

\textbf{Internalization:} To bridge this gap we introduce \emph{latent state internalization}, a novel paradigm in which an LLM reasons jointly with a non language subagent over a single reasoning trace of \lang{language}, \action{action}, and \state{latent states}. As shown in \cref{fig:LLAMIA}, the paradigm involves a closed loop of three distinct steps: \circled{1} LLM reasons in language and generates actions that evolve the environment state, exploring counterfactual environment states, \circled{2} Invoke the sub-agent to query for its internal state on demand, and \circled{3} the subagent's internal state get appended into the CoT as latent tokens. This results in an evolving and unified CoT of interleaved state, language, and action tokens that the LLM can be trained to reason on. To project the internal representation into the LLM's embedding space, we train a lightweight module over the agent's penultimate layer we call \textbf{LatentBridge} (\cref{xxxstage1})   

\textbf{LLAMIA:} We instantiate latent state internalization by training LLMs \citep{qwenXXX} in two stages: self-supervised pretraining followed by reinforcement learning detailed in \cref{XXXstage2}. We call the resulting model LLAMIA (Large Language and Action Models with Internal Agents). Beyond the technical contribution, internalization opens new avenues for real world applications. Because internalization requires access to model weights, it cannot be applied directly to closed-source frontier models, which due to safety, alignment, and availability are the primary application paths.
LLAMIA resolves this tension by acting as a bridge: it interacts with LLMs in natural language and internalizes the subagent, giving closed-weight models indirect but faithful access to the non language's expertise. This makes real-world creative applications like grounded game commentary and opponent-specific preparation deployable today, without retraining Frontier closed-weight models (\cref{XXXApplication}).

\textbf{Verbalization as the bottleneck:} To conclusively establish and analyze the effect of internalization, we train a controlled ablation, LLAMIA-Verb, in which the subagent's outputs are exposed to the LLM through verbalization, without the latent tokens. LLAMIA achieves substantially higher reward throughout the training. This gap widens on tasks that require deeper general and domain specific reasoning. These results show that verbalization acts as fundamental bottleneck in treating non-language agents as tools. We hope these results encourage further work on latent multi-agent systems.

\textbf{LLAMIA-Bench, Chess as a testbed:} Despite growing interest in latent agent communication~\cite{XXXlatentsurvey}, coordination with non-language agents remains underexplored. We hypothesize that a central reason is the lack of environments offering the combination needed to structurally test collaboration: diverse tasks with verifiable metrics, large-scale public data, and open pretrained agents. Chess uniquely provides all three. Humans have collaborated with engines for decades across commentary, opponent preparation, and puzzle curation, with established evaluation protocols, documented at scale through public game databases~\cite{Lichess,Caissabase}, forums \cite{Gameknot}, and annotated datasets~\cite{feng2024chessgpt}. We therefore use chess as our primary testbed and introduce \textsc{LLAMIA-Bench} (\cref{XXXDataset}), a curated suite of [XXX] established benchmarks spanning behavior cloning across skill levels, behavioral stylometry, puzzle interest and difficulty estimation, move annotation. We extend these with two new datasets and tasks: game level commentary from videos, and instruction-conditioned planning. To demonstrate further validity of internalization we also extend our results on poker XXX (\cref{pokerXXX}).

\textbf{Results:} We evaluate LLAMIA against dedicated task experts, frontier reasoning models with tool calling, and an RL-trained baseline (LLAMIA-Verb). A single LLAMIA model matches or exceeds every specialist and baseline across \textsc{LLAMIA-Bench}. We also find many interesting qualitative findings that distinguish LLAMIA from baselines: when imitating play, LLAMIA reproduces human-like tricks and blunders under time pressure. Its attention maps align more closely with human saliency than prior task experts across skill levels, and its commentary recovers the lines discussed by grandmasters at higher rates than any baseline. A human study confirms that internalized commentary is rated higher on both correctness and explanatory power. Finally, attention analyses and linear probes reveal that LLAMIA modulates its policy based on natural-language instructions. Lastly, we also observe that LLAMIA exhibits superior agency, as it solves problems in agent invokations and discovers unique collaboration patterns. 

\begin{enumerate}[leftmargin=*, labelsep=0.5em, topsep=2pt, itemsep=2pt]
\item \textbf{Latent state internalization} (Section~\ref{sec:methodology}): A novel paradigm for LLM non language agent collaboration, enabling LLMs to reason jointly over an interleaved chain of $\langle
\lang{language}, \action{action}, \state{latent state}\rangle$ tokens.
\item \textbf{LLAMIA} (Section~\ref{sec:methodology}): A two-stage training framework, self-supervised pretraining followed by end to end RL that yields a single model achieving state of the art performance across diverse collaborative tasks. We further show that LLAMIA can serve as an agent that gives closed weight frontier models faithful access to subagents enabling real world applications.
\item \textbf{Verbalization as the bottleneck} (Section~\ref{sec:experiments}): Through controlled ablations against LLAMIA-Verb, we show verbalized communication is suboptimal in both performance and compute, with the gap widening on tasks requiring deeper, multi-step collaboration.
\item \textbf{\textsc{LLAMIA-Bench}} (Section~\ref{sec:experiments}): A curated benchmark of [XXX] chess tasks spanning behavior cloning, stylometry, difficulty and interest estimation, commentary, instruction conditioned planning. We also evaluate on Poker XXX to demonstrate
\end{enumerate}

% \begin{enumerate}[leftmargin=*, labelsep=0.5em, topsep=2pt, itemsep=2pt]
%     \item \textbf{Latent state internalization (Section~\ref{sec:methodology}):} A novel paradigm for LLM and non-agent communication with direct latent representation internalization for multi-step planning and reasoning.

%     \item \textbf{\textbf{LLAMIA(Section~\ref{sec:methodology})}} We train LLMs with end to end RL over Chains of \lang{language}, \action{action}, and \state{latent state} tokens. The resulting subagent \textbf{LLAMIA}, achieves state-of-the-art performance across diverse tasks and baselines.\somesh{Application with Frontier LLMs}

%     \item \textbf{Verbalization as the bottleneck (Section~\ref{sec:experiments}):} We show through controlled ablations that verbalized communication is suboptimal both in terms of performance and compute efficiency, and the gap grows for tasks requiring deeper collaboration and multi-step interactions.

%     \item \textbf{\textsc{LLAMIA-Bench} (Section~\ref{sec:experiments}):} To evaluate LLM and domain-agent collaboration, we curate [XXX] chess benchmarks, spanning [XXX] diverse important tasks including behavior cloning, stylometry, difficulty estimation, commentary, and instruction-conditioned planning.
% \end{enumerate}

\vspace{-2mm}
\begin{figure}[!h]
    \centering
    \includegraphics[width=0.98\linewidth]{llamia-fig (5).pdf}
    \caption{\textbf{Latent State Internalization.} Given a state $S_0$ and a language instruction, the agent's encoder representation $h_{S_0}$ is projected into the LLM's embedding space via $H_\phi$ (indicated by \textcolor[HTML]{8C25FF}{\(\rightarrow\)}) as $K{=}32$ continuous tokens $z_{S_0}$, interleaved with language and \texttt{<action>} tokens. Stage~1 trains only the projector $H_\phi$ on state-policy pairs; Stage~2 fine-tunes both LLM and projector on diverse instruction data. At inference, the \texttt{<InvokeAgent>} token triggers dynamic re-encoding: the LLM's generated action transitions the state, and the agent produces fresh latent tokens for the updated position, enabling iterative reasoning over evolving states.
}
    \vspace{-3mm}
    \label{fig:LLAMIA}

\end{figure}